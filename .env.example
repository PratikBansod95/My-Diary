# Copy to .env for local `npm run dev` (never commit the real key).
OPENROUTER_API_KEY=
# Default (NVIDIA via OpenRouter):
OPENROUTER_MODEL=nvidia/nemotron-nano-12b-v2-vl:free
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
